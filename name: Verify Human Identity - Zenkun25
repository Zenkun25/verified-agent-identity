name: Verify Human Identity - Zenkun25

on:
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup & Verify
        run: |
          npm install -g clawhub
          npx clawhub@latest install verified-agent-identity
          echo "ClawHub installed successfully for Zenkun25"
          echo "Coba jalankan manual nanti di Codespaces atau Termux"
